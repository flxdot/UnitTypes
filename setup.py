from setuptools import setup, find_packages
import unittest

setup(
    name='pyUnitTypes',
    version='0.0.1',
    description='python package to work with different physical units as types and pythons type annotations',
    packages=['pyUnitTypes'],
    test_suite='tests'
)



